backend:
  mode: skyway2023
  # Deploy your own https://github.com/TK11235/udonarium-backend
  # ACCESS_CONTROL_ALLOW_ORIGIN must match this site's Origin (e.g. https://localhost:4200).
  # Do NOT use nanasunana public workers URL from localhost — it returns 403.
  url: https://{your-backend-hostname}/ #Your Backend API URL
app: #未使用
dice:
  url: 